---
interface Props {
	tag: string;
	withHref?: boolean;
}

const { tag, withHref } = Astro.props;
---

<span
	class={`mb-2 mr-2 rounded-xl bg-gray-600 px-3 py-1 text-xs font-bold uppercase text-white group-hover:bg-gray-500 ${
		withHref ? 'hover:bg-gray-500' : ''
	}`}
>
	#{tag}
</span>
